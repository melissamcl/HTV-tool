import React, { useState } from 'react';

function Inventory() {
  // iterate over all colors in db
  // for each color, create circle with bg color set to hex
  // on hover, display properties (brand + style, color name)

  // options at top of page to filter by brand/style
  return (
    <div>
      <h1>Inventory</h1>
    </div>
  );
}

export default Inventory;
